# Data dictionary — CCRA marts

| | |
|---|---|
| **Document ID** | CCRA-DD-001 |
| **Generated** | From the live warehouse schema |

Generated by `scripts/generate_data_dictionary.py` directly from
`information_schema`, so it cannot drift from the actual model.
Regenerate after any schema change.

Business rule references (BR-nn) point to `02-business-requirements.md`.

---

## `dim_account`

One row per credit facility, denormalised with borrower attributes.

**Rows:** 80,528

| Column | Type | Notes |
|---|---|---|
| `account_key` | BIGINT | Surrogate key. |
| `account_id` | BIGINT |  |
| `borrower_key` | BIGINT | Surrogate key. |
| `product_key` | VARCHAR | Surrogate key. |
| `geography_key` | VARCHAR | MD5 surrogate key over (province_code, cma_name). |
| `origination_date` | DATE |  |
| `origination_vintage_year` | TIMESTAMP |  |
| `origination_rate_era` | VARCHAR | Rate environment at origination. LOW_RATE_ERA carries the renewal risk. |
| `renewal_date` | DATE |  |
| `term_months` | INTEGER |  |
| `amortization_months` | INTEGER |  |
| `is_amortising` | BOOLEAN | Boolean flag. |
| `is_revolving` | BOOLEAN | Boolean flag. |
| `is_renewable` | BOOLEAN | True where term < amortisation, so the facility reprices rather than maturing. |
| `original_balance_cad` | DOUBLE | Amount in CAD. |
| `original_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `original_payment_cad` | DOUBLE | Amount in CAD. |
| `ltv_at_origination` | DOUBLE |  |
| `ltv_band` | VARCHAR |  |
| `property_value_cad` | DOUBLE | Amount in CAD. |
| `tds_at_origination` | DOUBLE |  |
| `credit_risk_band` | VARCHAR | BR-03 banding of credit score at origination. |
| `income_band` | VARCHAR |  |
| `employment_type` | VARCHAR |  |
| `is_newcomer` | BOOLEAN | Boolean flag. |
| `province_code` | VARCHAR |  |
| `cma_name` | VARCHAR |  |

## `dim_borrower`

One row per borrower. Attributes are as-at origination.

**Rows:** 60,000

| Column | Type | Notes |
|---|---|---|
| `borrower_key` | BIGINT | Surrogate key. |
| `borrower_id` | BIGINT |  |
| `province_code` | VARCHAR |  |
| `province_name` | VARCHAR |  |
| `cma_name` | VARCHAR |  |
| `geography_key` | VARCHAR | MD5 surrogate key over (province_code, cma_name). |
| `credit_score_at_origination` | INTEGER |  |
| `credit_risk_band` | VARCHAR | BR-03 banding of credit score at origination. |
| `annual_income_cad` | DOUBLE | Amount in CAD. |
| `income_band` | VARCHAR |  |
| `age_band` | VARCHAR |  |
| `employment_type` | VARCHAR |  |
| `is_newcomer` | BOOLEAN | Boolean flag. |

## `dim_date`

Conformed monthly date dimension. Mark as the date table in Power BI.

**Rows:** 42

| Column | Type | Notes |
|---|---|---|
| `date_key` | DATE | Surrogate key. |
| `date_month` | DATE |  |
| `calendar_year` | BIGINT |  |
| `calendar_month` | BIGINT |  |
| `calendar_quarter` | BIGINT |  |
| `month_label` | VARCHAR |  |
| `year_month` | VARCHAR |  |
| `quarter_label` | VARCHAR |  |
| `fiscal_year` | BIGINT |  |
| `is_current_month` | BOOLEAN | Boolean flag. |

## `dim_geography`

Province and census metropolitan area, with latest regional macro readings.

**Rows:** 16

| Column | Type | Notes |
|---|---|---|
| `geography_key` | VARCHAR | MD5 surrogate key over (province_code, cma_name). |
| `province_code` | VARCHAR |  |
| `province_name` | VARCHAR |  |
| `cma_name` | VARCHAR |  |
| `region_name` | VARCHAR |  |
| `latest_unemployment_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `latest_house_price_index` | DOUBLE |  |

## `dim_product`

Product catalogue with reporting groupings.

**Rows:** 5

| Column | Type | Notes |
|---|---|---|
| `product_key` | VARCHAR | Surrogate key. |
| `product_code` | VARCHAR |  |
| `product_name` | VARCHAR |  |
| `product_category` | VARCHAR |  |
| `is_revolving` | BOOLEAN | Boolean flag. |
| `is_renewable` | BOOLEAN | True where term < amortisation, so the facility reprices rather than maturing. |
| `display_order` | INTEGER |  |

## `fact_account_month`

Central fact. Grain: one row per account per reporting month.

**Rows:** 3,008,785

| Column | Type | Notes |
|---|---|---|
| `account_key` | BIGINT | Surrogate key. |
| `date_key` | DATE | Surrogate key. |
| `borrower_key` | BIGINT | Surrogate key. |
| `product_key` | VARCHAR | Surrogate key. |
| `geography_key` | VARCHAR | MD5 surrogate key over (province_code, cma_name). |
| `months_on_book` | INTEGER |  |
| `balance_cad` | DOUBLE | Outstanding balance. Additive across every dimension. |
| `scheduled_payment_cad` | DOUBLE | Amount in CAD. |
| `interest_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `payment_shock_pct` | DOUBLE | Realised payment increase at renewal: new payment / original payment - 1. |
| `payment_shock_band` | VARCHAR |  |
| `has_renewed` | BOOLEAN |  |
| `delinquency_state` | VARCHAR | CURRENT, DPD_30, DPD_60, DPD_90, DPD_120 or DEFAULT. DEFAULT is absorbing. |
| `days_past_due_bucket` | VARCHAR |  |
| `is_delinquent` | BOOLEAN | Any state other than CURRENT (BR-02). |
| `is_impaired` | BOOLEAN | 90+ days past due or defaulted (BR-01). |
| `is_default` | BOOLEAN | Boolean flag. |
| `monthly_pd` | DOUBLE | Modelled monthly probability of entering delinquency. |
| `expected_loss_cad` | DOUBLE | Modelled expected credit loss = exposure x PD x LGD. |
| `unemployment_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `delinquent_balance_cad` | DOUBLE | Balance where state <> CURRENT. Numerator for the balance-weighted delinquency rate. |
| `impaired_balance_cad` | DOUBLE | Balance 90+ DPD or defaulted (BR-01). |
| `defaulted_balance_cad` | DOUBLE | Balance in the absorbing DEFAULT state. |
| `delinquent_account_count` | INTEGER |  |
| `impaired_account_count` | INTEGER |  |
| `account_count` | INTEGER |  |

## `agg_portfolio_monthly`

Portfolio summary by month, product, province, vintage and risk band.

**Rows:** 32,036

| Column | Type | Notes |
|---|---|---|
| `snapshot_month` | DATE |  |
| `product_name` | VARCHAR |  |
| `product_category` | VARCHAR |  |
| `province_code` | VARCHAR |  |
| `origination_rate_era` | VARCHAR | Rate environment at origination. LOW_RATE_ERA carries the renewal risk. |
| `credit_risk_band` | VARCHAR | BR-03 banding of credit score at origination. |
| `account_count` | BIGINT |  |
| `total_balance_cad` | DOUBLE | Amount in CAD. |
| `delinquent_balance_cad` | DOUBLE | Balance where state <> CURRENT. Numerator for the balance-weighted delinquency rate. |
| `impaired_balance_cad` | DOUBLE | Balance 90+ DPD or defaulted (BR-01). |
| `defaulted_balance_cad` | DOUBLE | Balance in the absorbing DEFAULT state. |
| `expected_loss_cad` | DOUBLE | Modelled expected credit loss = exposure x PD x LGD. |
| `delinquent_account_count` | HUGEINT |  |
| `impaired_account_count` | HUGEINT |  |
| `avg_interest_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `avg_monthly_pd` | DOUBLE |  |
| `avg_unemployment_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `renewed_balance_cad` | DOUBLE | Amount in CAD. |
| `avg_payment_shock_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |

## `agg_renewal_exposure`

Forward-looking payment shock for facilities not yet repriced.

**Rows:** 32,461

| Column | Type | Notes |
|---|---|---|
| `account_key` | BIGINT | Surrogate key. |
| `account_id` | BIGINT |  |
| `product_key` | VARCHAR | Surrogate key. |
| `province_code` | VARCHAR |  |
| `cma_name` | VARCHAR |  |
| `credit_risk_band` | VARCHAR | BR-03 banding of credit score at origination. |
| `income_band` | VARCHAR |  |
| `origination_rate_era` | VARCHAR | Rate environment at origination. LOW_RATE_ERA carries the renewal risk. |
| `ltv_band` | VARCHAR |  |
| `renewal_date` | DATE |  |
| `renewal_quarter` | TIMESTAMP |  |
| `balance_cad` | DOUBLE | Outstanding balance. Additive across every dimension. |
| `original_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `current_rate_pct` | DOUBLE | Percentage, expressed as a decimal where it is a ratio. |
| `assumed_renewal_rate_pct` | DOUBLE | Latest observed conventional 5-year rate, used as the repricing assumption (BR-08). |
| `current_payment_cad` | DOUBLE | Amount in CAD. |
| `projected_payment_cad` | DOUBLE | Amount in CAD. |
| `payment_increase_cad` | DOUBLE | Amount in CAD. |
| `projected_payment_shock_pct` | DOUBLE | Projected increase at the assumed renewal rate, for facilities not yet repriced. |
| `projected_shock_band` | VARCHAR | Severity banding of projected shock (BR-05). |
| `current_monthly_pd` | DOUBLE |  |
| `delinquency_state` | VARCHAR | CURRENT, DPD_30, DPD_60, DPD_90, DPD_120 or DEFAULT. DEFAULT is absorbing. |

---

## Quality results

### `quality.dq_results`

One row per rule per pipeline run. Quality as a time series.

| Column | Type | Notes |
|---|---|---|
| `run_timestamp_utc` | TIMESTAMP WITH TIME ZONE | Pipeline run that produced this result. |
| `rule_name` | VARCHAR | Rule identifier. |
| `dimension` | VARCHAR | completeness | uniqueness | validity | consistency | reconciliation |
| `severity` | VARCHAR | error breaches stop the build; warn are recorded only. |
| `metric_value` | DOUBLE | Measured value. |
| `threshold` | DOUBLE | Configured limit from config/pipeline.yml. |
| `direction` | VARCHAR | max: pass when metric <= threshold. min: pass when metric >= threshold. |
| `passed` | BOOLEAN | Rule outcome. |
| `description` | VARCHAR | What the rule protects against. |
